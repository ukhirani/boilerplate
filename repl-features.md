# REPL (Read Evaluate Print Loop)

we can add params for interpolation, which will be an array of string in the config

like :
params = [["name","surname"]]

then we can do a REPL asking for each of these params :

> Enter name :
> Enter surname :

same can be integrated with the "clip" command
